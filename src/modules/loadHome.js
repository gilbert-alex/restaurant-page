// loadHome.js

// Append home data to html '#content' container.
// json loader invoked within index.js converted the json data to a js obj
// Side effect only. No return value.

const loadHome = function (info) {
    console.log(info);
}

export default loadHome;